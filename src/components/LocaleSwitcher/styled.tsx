import { Dropdown } from '@swingby-protocol/pulsar';
import styled from 'styled-components';

export const Target = styled(Dropdown.DefaultTarget)`
  background: transparent;

  :active,
  :hover {
    background: transparent;
  }
`;
